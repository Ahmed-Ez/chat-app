<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- Compiled and minified CSS -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Baloo+Tamma+2&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="./css/styles.css" />
    <title>A.EZ|Register</title>
  </head>
  <body>
    <div class="container">
      <div class="card">
        <div class="card-content">
          <h1>Register</h1>
          <form method="post">
            <div class="input-field">
              <input
                placeholder="name"
                name="name"
                id="name"
                type="text"
                required
              />
              <label deep-orange lighten-1 for="name">Display Name</label>
            </div>
            <div class="input-field">
              <input
                placeholder="Email"
                id="email"
                name="email"
                type="email"
                required
              />
              <label deep-orange lighten-1 for="email">Email</label>
            </div>
            <div class="input-field">
              <input
                placeholder="password"
                name="password"
                id="password"
                type="password"
                required
              />
              <label deep-orange lighten-1 for="password">Password</label>
            </div>

            <button
              class="waves-effect waves-light red btn"
              id="btn"
              type="submit"
            >
              <i class="material-icons">chat_bubble</i>
              Sign Up
            </button>
          </form>
          <span>Already Have An Account ?<a href="/login"> Sign In</a></span>
        </div>
      </div>
    </div>

    <!-- Compiled and minified JavaScript -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
  </body>
</html>
